# Minimal requirements for MineStudio + STEVE-1
# Install these first, then install minestudio separately

# PyTorch (CPU/MPS version for macOS)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Core dependencies
numpy>=1.24.0
pillow>=9.0.0
opencv-python>=4.8.0
huggingface-hub>=0.19.0
rich>=13.0.0
tqdm>=4.65.0
einops>=0.7.0
psutil>=5.9.0

# Note: Install minestudio separately after these:
# pip install minestudio

