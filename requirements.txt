# Core dependencies for MineStudio + STEVE-1 + AgentBeats evaluation
# Python 3.10 recommended

# PyTorch (will auto-detect MPS on Apple Silicon)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Core scientific computing
numpy>=1.24.0
pillow>=9.0.0
opencv-python>=4.8.0

# MineStudio (includes most dependencies)
minestudio

# HuggingFace for model downloads
huggingface-hub>=0.19.0

# Utilities
rich>=13.0.0
tqdm>=4.65.0
einops>=0.7.0

# Optional but recommended
psutil>=5.9.0  # For memory monitoring

# Note: OpenJDK 8 must be installed separately via conda or Homebrew
# conda: conda install --channel=conda-forge openjdk=8 -y
# macOS: brew install --cask temurin8

